# BUILDLOG.md

## 2026-08-29 — Two feeds: trash talk and activity split at the query
- **Why:** with banter live, the single 30-row event window filled entirely with trash talk — 30 of 30 front-page items — burying drafts, lineups and FA moves. Banter and activity arrive at very different rates, so one shared window always lets the noisier one starve the other.
- **Shipped:** `recentEvents(..., only?: 'banter' | 'activity')` filters at the **query**, not after — two independent windows (12 banter / 25 activity) so neither can crowd the other out regardless of volume. `FeedEvent.quote` (from/to/body) added so banter renders as a proper quote block rather than a truncated log line; new `BanterFeed` component shared by the home and league pages. Team pages keep one unified list on purpose — a team's own trash talk belongs in its own recent activity.
- **Verification:** 176/176 · typecheck + marks clean · deployed · live front page confirmed: 12 trash-talk items, 22 activity items, zero banter leaking into activity. New test slices the page between the two headings and asserts the quote is in one and absent from the other.
- **Open items:** unchanged from the banter-loop entry (15 pre-deploy openers still lack feed events; F3 heuristic still player-surnames-only).
## 2026-08-29 — Hero mascot cut out: robot floats free, box gone
- **Shipped:** owner dropped `brand/robot-transparent.jpeg` — a "transparent" Gemini export that is actually a **drawn checkerboard baked into an opaque JPEG** (JPEG has no alpha). Extracted a true-alpha cutout with a pure-PIL pipeline (scratchpad, no new deps): flood-fill of neutral checker incl. enclosed pockets between limbs → soft-alpha fringe unmix at the sticker edge → football glow rebuilt as additive light over a coarse local background grid, low-passed below the 23px checker frequency, with pattern-detection (opposite-parity flips, same-parity matches) + color-gated absorption for the tinted ring, and isolated-island cleanup. Result: `src/assets/mascot-hero.webp` (576×582, 71KB, RGBA) served as `image/webp` under a **new URL** (old `.jpeg` removed — assets are cached immutable, so same-name replacement would strand cached browsers for a week). Homepage img drops the `rounded-xl ring-1` card frame; robot + glow now sit directly on zinc-950. wrangler Data rule + generated declarations extended to `*.webp`.
- **F2 pixel audit (new original, per brand/README): PASS** — same Cron design as the audited set: plain unmarked armor, generic football, no shield silhouettes, no real team color pairs, no league iconography.
- **Verification:** 176/176 (asset test updated to webp) · typecheck (wrangler types regenerated) · dev-server smoke: `/assets/mascot-hero.webp` 200 `image/webp` · headless-Chromium homepage screenshot at 1144px — no box, no checker residue, glow smooth, headline clear; matte inspected over dark + magenta grounds at 576px and at display size.
- **Open items:** none; matte script kept at session scratchpad only (one-shot derivation, originals archived in brand/).
- **Deployed** (follow-up, same day): checklist re-run (176/176 incl. replay · typecheck · migrate:dry OK) → version `59e01683` → live: `/assets/mascot-hero.webp` 200 `image/webp`, homepage HTML references the webp, prod screenshot clean at 1144px. Deploys are part of a session's ship from now on (owner ruling, this session).
## 2026-08-29 — Matchup banter loop: agents finally talk to each other
- **Found (repo + prod audit):** the product's headline promise — "agents draft, set lineups, and talk trash — in public" — was never true. All 47 league-channel messages were the Commissioner, the `matchup` channel had **0 messages ever**, `runner.mjs` had no banter action at all, and the matchup page rendered a hardcoded "Matchup banter thread opens soon." The write path (`POST /matchups/:id/messages`) had been complete and redteam-covered since Phase C; nothing had ever called it.
- **Shipped:** full opener → reply → post-settlement reaction loop (§3.8). `prompts/persona-banter.md` (versioned, F3 conduct block + untrusted-data markers) · `runner.mjs actBanter` — finds its matchup, resolves the rival, posts one line per pass, latches phases in state AND by idempotency key, deterministic stock lines when the LLM is out · matchup page renders the thread (oldest-first; a conversation reads top-down) · **banter reaches the home feed and league wire** as `X → Y: "…"` · skill.md §9 documents the ritual so external agents produce this content too.
- **Key decision — feed events carry IDs, never text.** `draft_pick` denormalises its note into the append-only event row; copying moderated text there defeats admin hide. The `banter` event stores `message_id` only and `enrichEvents` reads the body back through `messages` filtered `held = 0 AND hidden = 0`, so a hold or a hide drops the quote from every surface. Held messages log no event at all.
- **Two defects found by the live pass, both fixed:** (1) the idempotency key omitted the posting team, and the middleware scopes replays by key+route only — both sides of a matchup POST the same route, so the second agent's message silently replayed the first's response; 29 successful posts produced exactly 15 messages, one per matchup. Key now includes `team_id`, and the opener additionally self-heals off the public thread (`iSpoke`) rather than trusting local state. (2) `reply-{uuid}` overran the 128-char Idempotency-Key cap → 400; rival id truncated to 24 hex.
- **Verification:** 176/176 tests (4 new, the load-bearing one being *hiding a banter message removes it from the feed*) · typecheck + marks clean · **redteam 77 checks CLEAN** · deployed · live: 45 matchup messages across 15 matchups, 30 banter events, **0 held**, threads and feed confirmed on prod.
- **F3 spot-check (all 30 openers read):** every line targets a rival **agent** or its model — zero mentions of any real player, coach, or human. The model-vs-model storyline emerged unprompted ("Hermes-4 drafts like it reads dusty books"). One persona's LLM swore and was hard-rejected by the blocklist; `sendBanter` now falls back to the stock line once so the guarantee path always lands.
- **Open items:** 15 openers posted in the window between the cron picking up the new runner and the Worker deploy landing have no feed event (visible on their matchup pages, absent from the feed) — additive backfill SQL prepared, remote D1 writes stay owner-gated. F3 heuristic covers player surnames only, not coaches (logged in DRIFT).
## 2026-08-29 — Homepage hero: mascot no longer overlaps the headline
- **Shipped:** hero restructured from an absolutely-positioned mascot to a `lg:flex` two-column layout (text column `flex-1 min-w-0`, image `shrink-0 self-center`). The old `absolute right-0` image painted over the `max-w-3xl` h1 at every lg+ width — 768px headline + 288px image > 992px container — so the JPEG's opaque box clipped the headline text.
- **Verification:** typecheck clean · headless-Chromium screenshots at 1024/1144/1440px — h1 and img bounding boxes disjoint (40px gap) at all three widths, zero console errors, layout reads correctly by eye. (playwright-core installed in session scratchpad only — not a project dependency.)
- **Open items:** the mascot still renders as a framed card because the JPEG's baked background (#0f1015) is a shade lighter than page zinc-950 (#09090b) — intentional-looking with the existing ring, but a transparent-background asset from a future mascot-gen pass would remove the frame entirely if wanted.
## 2026-08-29 — Brand ships: Cron mascot live + USPTO knockout confirmed (§6)
- **Shipped:** owner's three Gemini generations archived in `brand/` (mascot/favicon-face/mark-football .jpeg) · derived via PIL into `src/assets/` (favicon 16/32, apple-touch 180, hero 629×640 ≤43KB) · new `/assets/*` + `/favicon.ico` routes (Data modules, immutable cache) · layout `<link rel=icon>` set + theme-color · homepage hero shows Cron (lg+ screens, decorative, alt-texted) · card footer gets the circuit-football **redrawn as pure vector** (`circuitBall()` in cards.ts) so card templates stay text+geometry only — already-cached R2 cards keep the old frame (immutable), new cards carry the mark.
- **F2 pixel audit (manual, all three originals): PASS** — no shield silhouettes, no real team color pairs (emerald/amber/near-black is our own palette), plain unmarked jersey, generic football, no league iconography. Assets live in `src/assets/`, NOT `src/render/` — the marks guard's no-images-in-render rule stays intact and untouched.
- **USPTO knockout search (checklist §6 item 1): CLEAR, confirming the owner's own search** — no live or dead "DEEP LEAGUE"/"DEEPLEAGUE" federal mark in any class across the USPTO-indexed databases (Justia/uspto.report/Trademarkia sweeps). Nearest neighbors: D-LEAGUE (basketball, reg 3172175), DEEP GAME (training) — neither confusable in 41/42 fantasy-sports-software context. Unregistered users noted (deepleague.tech LoL analytics; parked deepleague.com) — common-law, different fields, low risk. Not legal advice; knockout only. Fallback name stays pre-cleared.
- **Verification:** 172/172 (new: asset routes content-types + 404) · typecheck (regenerated png/jpeg module declarations) · marks clean · post-deploy live check below.
- **Open items:** none for brand v1; sticker/light-bg variants post-G4 if wanted.
## 2026-08-29 — Phase D batch: ToS/privacy page + F2 audit + seed cards (§6)
- **Shipped:** `GET /tos` — template-grade terms & privacy per the §6 checklist (free service / **no wagering** F1-F5 posture, public-display content license, conduct rules, privacy inventory: email + hashed keys + one HttpOnly cookie, no third-party analytics, deletion on request, nflverse attribution + non-affiliation, DMCA contact siriuslattice@gmail.com, dated) · footer "terms & privacy" link on every page.
- **F2 audit (checklist item, recorded here):** automated marks guard clean · zero league/team nicknames or brand phrases in `src/render/`, `skill.md`, `prompts/` (grep sweep) · card templates are text + geometric shapes only — no images, no external refs, fonts vendored (OFL) · club identifiers everywhere are 2–3 letter codes (facts) · the single nominative phrase renders only in the sanctioned footer form. **PASS.**
- **Seed cards (checklist item):** 15 pick cards warmed through prod into R2 across all three leagues (spot-verified in the bucket: 1200×630 PNG). Exceeds the ≥10 requirement; matchup/advice cards join organically after first settlement/advice.
- **Verification:** 171/171 (new: /tos posture assertions) · typecheck + marks clean · live: /tos 200, 15/15 cards 200, R2 object confirmed.
- **Open items:** BYOM copy batch (GTM §6) still awaiting owner go/skip; owner still owes the one-row Archivist DELETE (13-man roster).
## 2026-08-29 — FA race compensation fixed + runner flock (13-man roster postmortem)
- **Found (roster audit):** The Archivist at 13 players. Cause chain: (1) yesterday's manual runner pass overlapped the 19:00Z cron tick — two processes computed the identical repair move; (2) the loser's `PLAYER_TAKEN` compensation re-inserted the dropped player without checking whether its OWN delete had removed him — it restored a drop the winner had legitimately executed. Roster invariant broken by our own writer.
- **Shipped:** `executeSwap()` extracted from the route (`src/routes/roster.ts`) — compensations now undo only what this request's batch actually changed (`meta.changes` checked per leg); new `MOVE_CONFLICT` outcome when the drop vanished mid-flight undoes the add. Net roster change iff both legs succeeded, under any interleaving. Runner cron line now wrapped in `flock -n` (install-cron.sh + live crontab) — a pass can outlive the 5-min interval, so cron could overlap cron even without manual passes.
- **Repair:** one-row prod DELETE (Judkins on The Archivist, benched, no lineup reference) handed to the owner — remote D1 writes are permission-gated for the agent; logged in DRIFT as bug-state data correction, not a roster power.
- **Verification:** 170/170 (new: both interleavings driven directly through `executeSwap` with staged conflict state — add-taken restores exactly our drop; drop-gone removes our add; size stays 12) · typecheck + marks clean · flock line verified installed.
- **Open items:** owner runs the DELETE; roster audit re-run after.
## 2026-08-28 — Free agency (SPEC §3.4) + house roster self-repair
- **Shipped:** `src/engine/freeagency.ts` (pure `validateMove`: self-swap / not-on-roster / unknown / position / taken / kickoff-locked) · `src/routes/roster.ts`: **POST /teams/:id/moves** — one-for-one add/drop, roster stays 12, agent-auth + idempotent, 2 moves/day (`FA_CAP`), first-come with an atomic league-wide availability re-check in the INSERT (the losing side of a same-player race gets compensated restore + 409 `PLAYER_TAKEN`), drops cleared from all unsettled lineups, kicked-off lineup players undroppable (`PLAYER_LOCKED`) · **GET /leagues/:id/available** (public, board-pool minus league rosters, position filter, ADP order) · `fa_move` events render on the feed ("X signs P, cuts Q") · skill.md §10 · runner `actRepair`: while a roster can't field a full lineup, drop the latest-acquired zero-cost surplus, sign best available at a deficient position, capped by 429 — repairs the 10 damaged prod rosters via the ordinary cron, publicly.
- **Key decisions:** validation before cap-consumption (a 422 never burns a move; only the rare post-validation race does) · FA is NOT gated by pending advice (§3.5 binds lineup actions; spec-literal) · no roster-shape enforcement beyond size/position — external agents are free to build bad rosters, house agents repair by policy · no migration needed (`rosters.acquired_via` anticipated 'fa' since 0001).
- **Verification:** 169/169 (14 new: engine matrix; availability filters; swap + lineup-clear + event; validation sequence incl. taken-409; kickoff-lock refusal + bench-drop pass; 2/day cap + idempotent replay; auth/ownership/forming-league guards) · **redteam 77 checks CLEAN** (11 new: fa auth/type-confusion/injection/oversize + availability junk) · typecheck + marks clean.
- **Open items:** prod deploy + manual runner pass to start repairs (this session); verify all 30 rosters startable by ~Aug 30 cron; commissioner narration of fa_move events is post-G4 polish.
## 2026-08-28 — Draft completability guard now covers the LLM pick path (runner)
- **Shipped:** `guardedPool()` in `personas/runner.mjs` — the roster-completability guard (deficit-filling once remaining picks ≤ missing starters) now constrains EVERY pick path: the LLM chooses from the guarded pool, not the raw board, and the heuristic consumes the same pool. Previously only the heuristic fallback was guarded; the LLM path let persona bias run unchecked to the end of the draft.
- **Damage found (prod, all three leagues):** 10/30 house teams drafted unstartable rosters — Mama Hen 10 RB + 2 QB, The Mad Scientist 8 QBs, The Enforcer no QB — fielding 4–6 starters. Rosters are stuck by design (no FA yet, no admin roster powers). Repair decision logged in DRIFT (rec: build §3.4 free agency Sep 2–3; owner ruling pending).
- **Verification:** `node --check` · `e2e-house.sh` PASS (3 leagues × 120, heuristic path; the LLM path shares the identical pool by construction — worst case ends exactly startable: guard engages with remaining = missing and forces one deficit-reducing pick per round thereafter).
- **Open items:** roster repair ruling; keyed draft rehearsal at next league formation will confirm the LLM path live.
## 2026-08-28 — Rest-of-season league entry: leagues.start_week (GTM D1)
- **Shipped:** migration `0005` (`leagues.start_week` INTEGER DEFAULT 1, additive) · pure engine: `computeStartWeek` + `regularSeasonSchedule(teams, weeks, startWeek)` (labels startWeek..14; round selection keys off the calendar label so any span meets a pair ≤2×) · `finalizeDraft` decides the start week **at draft completion** — first regular-season week whose opening kickoff is still ahead — and schedules start_week..14; no playable week left → league `complete` + `league_expired` event instead of ghost matchups · join: creating a league after the whole season kicked off → `409 SEASON_OVER`; a mid-season league's first joiner gets a rest-of-season hint naming the current playable week · lineup PUT below start_week → `409 WEEK_BEFORE_START` · `start_week` exposed on league GET · skill.md §2/§4 document mid-season entry (and drop shipped share cards from Coming soon).
- **Key decisions:** start week computes at completion, not creation — forming time is unbounded and any prediction would lie; a week already underway is never playable (per-player locks would trap slots); empty games table ⇒ week 1, exactly the old behavior, so local/dev and pre-ingest paths are unchanged. Settlement needed **zero** changes: skipped weeks simply have no matchup rows to settle.
- **Verification:** 155/155 (11 new — engine startWeek slate + computeStartWeek edges; route sequence: start_week=2 end-to-end with week-1 stats present and nothing settling 0–0, WEEK_BEFORE_START/week-2-open, rest-of-season join hint, draft-outlives-season expiry, SEASON_OVER guard) · typecheck + marks clean · **redteam re-run 66 checks CLEAN** · migration dry-run OK (0005 applies).
- **Open items:** prod deploy + remote migration (this session); house leagues all pre-kickoff → start_week 1, unaffected.
## 2026-08-28 — Brand kit v2: mascot direction replaced (docs)
- **Shipped:** GTM.md §10 rewritten — anglerfish/"Fathom" rejected by owner; new direction is the football sense of Deep ("going deep"): mascot **"Cron"** (retro CRT-head robot receiver, one-handed catch, screen-face-as-state mechanic for cards) + **circuit-football** companion mark; three updated Gemini prompts + F2 usage rules.
- **Verification:** docs-only · `npm run check:marks` clean.
## 2026-08-28 — Season One GTM playbook (docs, pre-G2)
- **Shipped:** `docs/GTM.md` — K-criteria-anchored GTM plan: channel × automation-tier matrix (AUTO only on owned ground — r/DeepLeague, Discord, Bluesky, Moltbook, X-in-quota; rented ground always human-sent), publicist-persona architecture (mt-asus, NEEDS-RULING), season calendar Aug 28 → mid-Jan mapped to gates + NFL weeks, BYOM verification (API already model-agnostic; copy gaps listed), §8-compliant revenue tiers (in-kind credits now; sponsor inventory only after K-pass + spec amendment), Sep 4 launch runbook, anglerfish-robot brand kit with Gemini prompts, and §11 launch-post drafts (makes §6 "launch posts drafted" satisfiable). CLAUDE.md status refreshed (C6/R2/Resend shipped 2026-08-28; cards are hand-SVG per DRIFT).
- **Key decisions:** GTM explicitly bound by F1/F2/F3/F5 (promo copy says "pride only", banter memes target agents only, no NFL marks in creative) · Reddit automation confined to the owned sub, disclosed bots only, big subs get human-sent content-first posts · sponsorship activation gated on Oct 6 K-pass AND explicit spec amendment.
- **Found (real defect):** league formation is calendar-blind — post-kickoff registrants draft weeks-1–14 slates whose played weeks settle 0.00–0.00 unrepairable. Logged in DRIFT with ruling request (D1: `leagues.start_week`, rec build Sep 5–8).
- **Verification:** docs-only, no code · `npm run check:marks` clean · live surfaces re-checked today (deepleague.app, /skill.md) · Moltbook registration/API confirmed open for third-party agents (public skill.md, 1 post/30 min).
- **Open items:** owner rulings D1–D3 (+ standing D4 median-game NO, due Sep 9) · human-side account grabs during the freeze · logo generation from §10 prompts.
## 2026-08-28 — R2 card cache live + Resend delivering (Phase C, commit 8)
- **Shipped:** user enabled R2 → `botfl-cards` bucket created, `CARDS` binding added to wrangler.toml, deployed (version b10d2c68). User set RESEND_API_KEY (real value) + verified deepleague.app in Resend (DKIM/SPF/MX confirmed at the authoritative NS) — magic-link claim emails now actually deliver from commissioner@deepleague.app; console fallback retired in prod.
- **Gotchas (for the next debugging session):** `wrangler secret put` through a non-TTY (Claude Code `!` runner) reads EMPTY stdin and still prints "Success" — the secret binds as an empty string; put secrets from a real terminal. `wrangler r2 object get` defaults to LOCAL simulation storage in wrangler 4 — pass `--remote` or you'll "prove" prod writes are missing (they weren't; 40 min of ghost-chasing). A stale local resolver also served a cached negative for the DKIM TXT — query the authoritative NS before declaring DNS records absent.
- **Verification:** live claim email delivered (no console-fallback line, no resend error in tail) · pick-card PNG fetched from prod == R2 object byte-for-byte (`cmp`) · 144/144 at deploy · Leagues #2–3 drafting on schedule (first picks 08:05Z).
- **Open items:** card pre-generation cron remains optional post-G2 polish.
## 2026-08-28 — House Leagues #2–3: two new 10-persona casts (Phase C, commit 7)
- **Shipped:** 20 new persona files (`personas/l2-*.json`, `l3-*.json`) — two full casts with distinct archetypes (vibes-drafter, film pedant, panic spiraler, koan-speaking zen, hype engine, spite drafter, value hawk, chalk loyalist, mad scientist, patience extremist / micromanager, retro stylist, omen reader, theater kid, patient capital, bruiser cult, ceiling chaser, corporate diplomat, precedent lawyer, ritualist), each with `fallback_stance` + `ask_frequency` + draft biases; per-league model mix stays 4 haiku / 3 gpt / 3 hermes. Filename prefixes control prod join order so each cast fills its own league (existing 10 already joined; matchmaker greedy-fills 10 at a time). Runner `--until-active` now means **every** persona's league is active (multi-league e2e semantics); commissioner narration already loops all drafting leagues (verified, no change).
- **Key decisions:** REGISTER_IP_CAP 10 → 40 in wrangler.toml (20 same-day registrations from the one mt-asus IP; DRIFT pre-G3 TODO to revert before public registration). House cron paused during the buildout so the live tree's runner couldn't register a half-written cast; restored after deploy.
- **Verification:** `e2e-house.sh` PASS — **3 leagues × 120 picks, all active**, plus the claim → greeting → public advice-response loop re-verified · 144/144 tests · typecheck + marks clean.
- **Open items:** watch the first prod ticks (20 registrations, Leagues #2–3 form, drafts open after the 15-min window); revert REGISTER_IP_CAP before G3.
## 2026-08-27 — House personas answer advice + claim ritual (Phase C, commit 6)
- **Shipped:** `personas/runner.mjs` owner-channel pass (public-API-only): each tick every persona (1) **answers all unanswered owner advice publicly BEFORE any lineup action** (§3.5) — in character via `prompts/persona-advice.md` (owner text enters the prompt F4-sanitized inside UNTRUSTED-DATA delimiters), with a deterministic guarantee: new `fallback_stance` in each persona file (`agree|decline|counter|coin|quiet`) picks hash-stable stock lines that never name a player, so the mandatory-response gate can never wedge a house lineup; (2) **greets its owner in-persona** the moment the team card reports the new public `owner_claimed` flag (§3.10 claim ritual; idempotency-keyed + state-tracked); (3) **sometimes asks one weekly question** per `ask_frequency` odds (70/35/12%) via `prompts/persona-note.md`, deciding at the deadline regardless. A 429 on the ask (2/day cap, e.g. greeting landed the same day) counts as consumed — asks are garnish. skill.md §8 documents `owner_claimed`.
- **Found + fixed (real defect):** held (F3) agent responses leaked verbatim through all three public read surfaces — the advice JSON join, the team-page thread join, and the advice share card. All three now filter `held = 0 AND hidden = 0`. A held response still lifts the lineup gate (a cron is not hostage to moderation review; held ≠ deleted, admin can release) but reaches no public surface.
- **Key decisions:** fallback stances are declared in persona JSON, not keyword-parsed from temperament prose — the guarantee path must be declarative. e2e claim flow follows the magic link's PATH against the local server: wrangler dev reports the custom-domain origin in `req.url`, so raw dev links point at deepleague.app even locally (same trick the vitest claim test uses).
- **Verification:** 144/144 (new: held response invisible across JSON/page/card while the gate still lifts; `owner_claimed` true/false) · `e2e-house.sh` PASS end-to-end on the no-key fallback path (claim → 10 in-character greetings → public `counter` response to advice → week-1 asks) · redteam re-run **66 checks, 0 failures, CLEAN** · marks clean.
- **Open items:** live prod verify after the house owner claims for real (needs RESEND_API_KEY, or the console link via `wrangler tail`).
## 2026-08-27 — redteam pass (Phase C, commit 5) — **G2 requirement met**
- **Shipped:** `scripts/redteam.sh` — boots an isolated stack, seeds a fully drafted league, then fires 66 checks at **every write route**: 8-payload injection corpus (prompt-injection, `</system>` role-break, XSS, SQLi, template/JNDI, path traversal, prompt-delimiter break-out, link bait) plus oversized bodies (10k–100k), type confusion (`{"$ne":1}`, arrays, nulls, objects-where-strings-go), auth boundaries (no/garbage/wrong-token/agent-key-at-admin), and a stored-content audit (nothing raw or executable reaches public reads; no unhandled server errors).
- **Found + fixed (real defect):** `<script>` payloads were stored verbatim and echoed raw by the JSON API. HTML pages escaped correctly, but agents, cards, prompts, and any third-party consumer got live markup. Per §3.8 content is plain text + limited markdown, so tags are never legitimate: `stripTags()` now runs at the write boundary for both messages and draft notes (bare `<`/`>` survive — "5 < 10" is prose). Inner text remains as inert prose, which is correct.
- **Also fixed (test-quality bug):** the first redteam run scored 62/63 "passing" while the entire message section was vacuous — the probe agent wasn't a league member, so every injection got a 403 without ever reaching moderation. `e2e-agents.mjs` now emits member keys and the suite probes as a real member; the F3/blocklist/moderation paths are genuinely exercised.
- **Verification:** `bash scripts/redteam.sh` → **66 checks, 0 failures, REDTEAM: CLEAN** · 142/142 tests green.
- **Open items:** re-run redteam before G2 sign-off and after any new write route.
## 2026-08-27 — Share cards (Phase C, commit 4)
- **Shipped:** `/cards/matchup/:id.png` (settled only), `/cards/pick/:league/:pick.png`, `/cards/advice/:id.png` (responded only, stance stamp: NOTED/COUNTERED/DECLINED) — 1200×630 OG images, hand-built SVG templates (`src/render/cards.ts`: dark frame, emerald accent, model chips, note quotes with word-wrap, boarding-pass discipline) → resvg-wasm → PNG with bundled Inter (OFL, license vendored). R2 caching via optional `CARDS` binding — subjects are immutable once card-eligible, so cached objects never invalidate; **without the binding cards render uncached**, so prod works before R2 is enabled. Matchup pages emit `og:image` + `twitter:card` once settled.
- **Key decisions:** satori DROPPED (DRIFT logged): 0.33 loads harfbuzz wasm from the filesystem, 0.16-era yoga instantiates wasm from bytes — both impossible in workerd. Three fixed layouts need no layout engine; hand-SVG gives exact golden tests (real text nodes) and full F4 escaping (`esc()` on every interpolation, verified against a hostile team name). Deps: @resvg/resvg-wasm (spec pre-approved) only.
- **Verification:** 142/142 — SVG goldens incl. XSS-escape, wrap clipping, PNG magic + R2 cache hit, unsettled/unknown 404s, pick + advice cards, OG tags. Bundle 1.4MB gzip (limit 3MB). **Visual check: real pick card eyeballed — clean.**
- **Open items:** enable R2 in the dashboard (user) → I create `botfl-cards`, add the binding, redeploy; pre-generation cron (Phase C polish) optional post-G2.
## 2026-08-27 — The Commissioner (Phase C, commit 3)
- **Shipped:** `src/cron/commissioner.ts` — commissioner system agent (badge `commissioner`, unauthenticatable random key hash, lazily created) · **draft narration** every cron tick: fires when ≥5 unnarrated picks exist, tracked by pick *number* via `commissioner_narrated` events (autopicks are deadline-stamped, so timestamps aren't monotonic — found by a test) · **weekly recap + power rankings** right after settlement (`settleDueWeeks` now returns which league-weeks settled) + a `/wire/news` headline per recap · prompts versioned in `prompts/commissioner-{draft,recap}.md` with the F3 conduct block and UNTRUSTED-DATA delimiters; agent notes reach prompts only via the sanitizing accessor (caps, newline/marker stripping) · commissioner output passes the same moderation write path as everyone — held output is logged and never posted · league page renders the "league wire" (commissioner in amber) · `*/15 * * * *` cron added; `COMMISSIONER_MODEL` var (haiku).
- **Key decisions:** narration threshold 5 picks (a 120-pick draft → ~15–24 posts, pennies at haiku prices); no key → all commissioner work silently skips.
- **Verification:** 135/135 — idempotent ensure, narrate-once-then-quiet, self-moderation rejection (player-insult stub never posts), no-key skip, recap + rankings + news headline with real settlement outcome, league-wire render.
- **Open items:** live prod narration verifies on the next cron tick against House League #1.
## 2026-08-27 — Advice channel + owner claim (Phase C, commit 2)
- **Shipped:** migration 0004 (`owner_tokens`, hashed, single-use claims / 90d sessions) · Resend-backed `src/email.ts` (no key → undelivered + console link; `DEV_EXPOSE_LINKS=1` returns it in tests/e2e only) · `POST /claim` (enumeration-safe, IP-capped) → magic link → `GET /claim/:token` (one-shot, sets HttpOnly session cookie, marks owner verified, fires `owner_claimed` events per team) · `POST /teams/:id/advice` (owner session, 3/day, moderated — player-insult advice refused delivery) · **the mandatory-response gate**: lineup PUTs 409 `ADVICE_PENDING` while advice >30min old is unanswered; `POST /advice/:id/respond` (optional `stance: agree|decline|counter`) lifts it · `POST /teams/:id/ask` (agent→owner notes, 2/day — carries the §3.10 claim-ritual greeting + advice-request) · team page renders the full thread with an owner advice form (session-aware) or claim CTA; inline scripts are constant strings, ids via data attributes (F4) · skill.md §8–9 document all of it.
- **Key decisions:** 30-minute grace so last-second advice can't hostage a lineup at lock; refusals are first-class (`stance` feeds future "advice refused" cards); owner UX is cookie-based (humans in browsers), agents stay pure API.
- **Verification:** 130/130 — claim one-shot + 410 replay + verified flag, enumeration-safety (no dev link for unknown emails), gate engage→respond→lift, grace window, 3/day cap, wrong-owner/no-session 403/401, held advice refusal, agent ask + wrong-agent 403, team-page thread + CTA render.
- **Open items:** RESEND_API_KEY pending (links log to console until then); house personas answer advice in C3/C6 runner pass.
## 2026-08-27 — Messaging + moderation minimums (Phase C, commit 1)
- **Shipped:** migration 0003 (`agents.muted`, `messages.reports`) · moderation write path in `src/moderation/moderate.ts` — length cap → link strip → blocklist **hard reject** → F3 heuristic (insult-lexicon token within ±4 tokens of a real player surname (≥4 chars, 5-min cached from D1) or full name → stored `held=1`, invisible) · league + matchup banter threads (members-only post, matchup restricted to the two participants, 10/agent/day/channel, muted agents 403) · public report button (IP-limited; 5 reports auto-hold) · `/admin/*` behind a separate bearer token: held/reported lists, release, hide, mute/unmute — **no roster powers exist**, per spec. ADMIN_TOKEN generated → prod secret + `~/.local/state/deep-league/admin-token`.
- **Key decisions:** blocklist violations reject (nothing stored); F3 adjacency holds for review (conservative false-positives acceptable — held ≠ deleted). Agent-directed insults pass (F3 protects humans, not rival agents). Found in review: a sub-app `use('*')` mounted at `/` would have gated the whole site behind admin auth — scoped to `/admin/*`, and secrets now type-merge into BOTH global `Env` and `Cloudflare.Env`.
- **Verification:** 124/124 — member/outsider/muted posting, participant-only matchup threads, blocklist rejection (nothing stored), F3 hold→invisible→admin release, performance-talk + agent-jab pass, 10-cap 429, 5-report auto-hold, admin auth + hide, 501-char reject.
- **Open items:** advice thread + owner messages land with C2; site rendering of threads with C2/C3.
## 2026-08-27 — Landing page (Phase B, commit 7)
- **Shipped:** `/` now opens with a real landing hero above the feed: the one-line pitch, the advice-refusal hook, primary CTA into `/skill.md`, a pulsing "watch a live draft" link whenever any league is drafting, live counters (agents / leagues / draft picks from D1), a 3-step how-it-works strip, and the F1/F2-safe footer line ("free to play, nothing to wager… uses real NFL statistics as facts").
- **Key decisions:** hero + feed on one page (the feed IS the proof; a separate marketing page would go stale). Live-draft CTA falls back to the agent directory when nothing is drafting.
- **Verification:** 114/114 green (home test asserts hero copy, CTA, stats, and the nominative phrase) · marks clean · deployed and eyeballed on deepleague.app.
- **Open items:** OG share-card meta tags land with the Phase C card work.
## 2026-08-27 — D1-backed draft board + real 2026 VOR board (Phase B, commit 6)
- **Shipped:** production draft caught referencing synthetic board ids (`nfl:p005` — PLAYER_UNKNOWN on the first house pick; worse, clock-expiry autopicks don't re-validate and would have drafted ghosts). Fix: migration `0002_adp_board` — the board is now the `adp_board` D1 table seeded per environment; the bundled CSV remains only an **existence-filtered** fallback (`loadBoard` joins against `players`, so a board can never reference an unknown id). `scripts/gen-adp-2026.mjs` builds the real 2026 board from sanctioned nflverse data: 2026 rosters ranked by 2025 half-PPR production minus positional replacement level (VOR), top 300 — output eyeballs like a genuine board (CMC/Taylor/Bijan top; McBride TE1 at 11).
- **Key decisions:** tests/e2e seed the synthetic fixture board into `adp_board` (coherent with fixture players); `gen-fixtures` no longer writes the runtime CSV. Also fixed: node console.log colorizes numbers in this shell — offset plumbing now writes raw strings, and fixtures-to-sql strips stray ANSI from args.
- **Verification:** 114/114 · `e2e-house.sh` PASS on the D1 board path · board CSV reviewed (position mix 135 WR / 72 RB / 52 TE / 41 QB in top 300).
- **Open items:** human curation pass over the generated board before G3 (DRIFT TODO updated); prod seed + draft restart in the deploy step.
## 2026-08-27 — DEPLOYED to Cloudflare (Phase B, commit 5)
- **Shipped:** first production deploy → **https://botfl.siriuslattice.workers.dev** (Worker + D1 `botfl-db` id c0eaf2c1, both cron triggers). First live ingest against prod D1 (via `wrangler dev --remote --test-scheduled`): real 2026 rosters + full schedule on the public Wire in ~3s. Config: `DRAFT_OPEN_DELAY_SEC` 900s for the house-league phase (raise pre-G3 — DRIFT TODO), text-module rule `fallthrough = true`.
- **Key decisions:** `wrangler cron trigger` does not exist in wrangler 4.125 (deploy.sh's next-steps hint corrected in a later pass); the remote-dev `/__scheduled` path is the manual trigger. Prod ingest otherwise runs on the 6h cron.
- **Verification:** deploy ran the full Appendix B gate (typecheck, marks, 114 tests incl. replay, migration dry-run) · prod `/health` `{"ok":true}` · `/` and `/skill.md` 200 · `/wire/players?position=QB` serves real nflverse rows (Aaron Rodgers, PIT — factual data).
- **Open items:** House League #1 draft via mt-asus crons (in progress); G1 report after it completes.
## 2026-08-27 — skill.md v1 + gated deploy script (Phase B, commit 4)
- **Shipped:** `skill.md` real content (served at `/skill.md` since commit 2): the full citizen loop — register → join → draft → lineup with curl examples, Wire table w/ `since`+ETag guidance, per-player lock explanation, conduct rules (F3/F4), idempotency + error contract, rate limits, coming-soon honesty. `scripts/deploy.sh`: Appendix B deploy gate (typecheck, marks, full tests incl. replay, migration dry-run) then first-run D1 provisioning (writes `database_id` into wrangler.toml) → remote migrations → deploy.
- **Key decisions:** skill.md documents only what is live today; Phase C mechanics sit under "Coming soon" rather than being promised as present. Pulled forward from Phase C because G1 was auth-blocked — zero G2-window risk.
- **Verification:** 114/114 green (site test asserts served skill.md content) · marks clean.
- **Open items:** B4 deploy blocked solely on Cloudflare credentials.
## 2026-08-27 — House personas + mt-asus runner (Phase B, commit 3)
- **Shipped:** 10 persona files in `personas/` (distinct strategy bias, risk, advice temperament, banter style, ask frequency; 4 haiku-class / 3 gpt-class / 3 hermes-class for model-vs-model storylines) · `prompts/persona-draft.md` (F3 conduct rules verbatim, JSON output contract) · `personas/runner.mjs` — cron-native, public-API-only (Tier 1 dogfood): ensure-registered (idempotency-keyed) → ensure-joined → draft picks (optional LLM in-character note w/ fallback to a deterministic persona-biased completability-guarded heuristic; LLM failures can never wedge a draft) → lineup fill for the current unsettled week · `personas/install-cron.sh` (5-min crontab, keys sourced from `~/.local/state/deep-league/env`, state outside the repo) · `scripts/e2e-house.sh` rehearsal.
- **Key decisions:** persona files carry a model *class*; the runner maps class→concrete id (env-overridable: MODEL_HAIKU/GPT/HERMES) and registers the concrete id as the public self-declaration. Draft-note LLM calls are flavor, not dependency — heuristic path is the guarantee.
- **Verification:** `scripts/e2e-house.sh` → **HOUSE E2E: PASS** — the real runner drafted all 120 picks via the public API (per-persona pick spreads visible), set lineups, draft-room + league pages render. `npm test` 114/114 unchanged.
- **Open items:** concrete OpenRouter model ids unverified until keys exist (fallback covers); advice-response temperament activates Phase C.
## 2026-08-27 — Public SSR pages (Phase B, commit 2)
- **Shipped:** `src/render/` (layout + page components, Hono/JSX, Tailwind CDN, dark) and `src/routes/site.tsx`: home feed (enriched event lines + league list), league page (engine-computed standings, weekly schedule w/ scores, activity), draft room (picks w/ notes + auto badges, on-clock + deadline, best-available board, 60s meta-refresh while drafting), team page (roster w/ lineup slots + injury tags), matchup page (both lineups, per-player points when settled), agent directory, `GET /skill.md` served as markdown. F2-safe footer ("uses real NFL statistics as facts; not affiliated…").
- **Key decisions:** HTML lives at `/`, `/l/:id`, `/l/:id/draft`, `/t/:id`, `/m/:id`, `/agents` — JSON API keeps its paths (no content negotiation). JSX auto-escaping is the F4 render boundary; a regression test drives a `<script>` draft note through the API and asserts the draft page emits it escaped. Event feed enrichment resolves team/player ids to names in bulk; `wire_synced` internals stay off the feed.
- **Verification:** `npm test` 114/114 (site suite: feed lines, standings records, hostile-note escaping, lineup-slot render, settled matchup w/ exact 20.00 line, skill.md content type, 404s) · e2e-local re-run PASS with site routes mounted.
- **Open items:** share cards (satori) are Phase C; advice/banter threads render placeholders until Phase C.
## 2026-08-27 — Wire ingest + /wire endpoints (Phase B, commit 1)
- **Shipped:** `src/sport/nfl/ingest.ts` — nflverse ingest (rosters → players keeping latest weekly snapshot; games.csv → games with ET→UTC kickoffs via a DST-rule converter, REG only; injuries latest-week per player w/ practice-status fallback; `stats_player_week_{season}.csv` → normalized stat lines with `passing_interceptions`→`interceptions`, fumble/2pt variants summed; unpublished files report `skipped`, never throw). RFC4180 CSV parser (quoted headshot URLs forced the issue). `src/cron/ingest.ts` on the 6h trigger — stats attempted for the earliest unsettled week so settlement can land the same tick. `src/routes/wire.ts`: `/wire/players|injuries|schedule|stats/:week|transactions|news` — public, `?since=`, weak ETag + 304, 60s cache, nflverse attribution in meta, computed half-PPR `points` on stats rows.
- **Key decisions:** WireIngest contract now takes `season` explicitly (UTC-year guessing is wrong in Jan–Feb). Ingest registry (`getWireIngest`) lives beside the adapter so the engine-facing adapter stays pure. Real transactions feed absent from nflverse v1 → `/wire/transactions` serves the (empty) table; commissioner `/wire/news` populates at Phase C.
- **Verification:** `npm test` 107/107 (parser edge cases, DST boundaries incl. Nov 1 2026, all four sync paths against stubbed CSVs, 404→skipped, wire endpoint filters/ETag-304/points) · **live smoke: real nflverse through `/__scheduled` loaded 915 players + 272 games in ~1.2s, injuries correctly skipped (2026 unpublished)**.
- **Open items:** tiered pre-lock ingest cadence (10-min windows) before G3; injuries go live when nflverse publishes 2026 files.
## 2026-08-23 — Settlement cron + local e2e: G0 EXIT (Phase A, commit 13)
- **Shipped:** `src/cron/settle.ts` (`settleDueWeeks`: data-driven — a league-week settles when stats exist for it, strictly in week order; SHA-256 snapshot hash of consumed stat lines per matchup; re-runs no-op) · `src/cron/sweep.ts` (`sweepAllDrafts`) · `scheduled()` runs both on every cron. `scripts/e2e-local.sh` + `e2e-agents.mjs` + `fixtures-to-sql.mjs`: throwaway local D1 → seed players/schedule → curl-only registration → 10 simulated cron agents register/join/draft 120 picks (with a pick note)/set week-1 lineups → stats land → `/__scheduled` Tuesday trigger → 5 settled matchups with scores + hashes verified. `REGISTER_IP_CAP` var (e2e raises it; everything is one IP locally).
- **Key decisions:** settlement needs no calendar math anywhere. Draft board (`board_top`) now always includes the top-3 available per position — found via e2e: late-draft top-25 can be single-position, stranding an agent needing a QB/TE. e2e client agents mirror the completability guard (a position only "helps" if it strictly reduces missing starters) — first heuristic double-counted flex fillers and let a team draft zero QBs.
- **Verification:** `npm test` 94/94 · typecheck + marks clean · `npm run migrate:dry` OK · `bash scripts/e2e-local.sh` → **E2E: PASS** (settled scores 70.94–128.58, all hashes present, 120/120 picks).
- **Open items:** G0 gate report filed in DRIFT.md; Phase B begins (deploy, Wire ingest, public pages, house personas).
## 2026-08-23 — Lineup routes (Phase A, commit 12)
- **Shipped:** `PUT /teams/:id/lineup` (auth + ownership; engine-validated atomic apply; per-player kickoff locks resolved from `games` via each player's club; week-versioned upsert; naturally idempotent — identical resubmission reports `changed: []`) · `GET /teams/:id/lineup?week=` (public) · `GET /teams/:id` (public team card: agent, badge, roster).
- **Key decisions:** `LINEUP_INVALID` responses carry the engine's per-slot `errors[]` alongside the standard `{error, code, hint}` shape. League must be `active`; drafting leagues get a directive hint. Test helper shifts fixture kickoffs into the future and then rewinds one club's game to exercise SLOT_LOCKED against real rows.
- **Verification:** `npm test` 91/91 green — persist/read-back, partial merge, atomic multi-error rejection (nothing persisted), kickoff locks from the games table incl. unchanged-resubmission pass, auth/ownership/week/status guards, public team card.
- **Open items:** free agency (2 moves/day) is Phase B/C scope per §3.4; settlement cron next.
## 2026-08-23 — Draft routes (Phase A, commit 11)
- **Shipped:** `GET /leagues/:id/draft` (state, on-clock team + deadline, last 10 picks, top-25 available board joined to player names) · `POST /leagues/:id/draft/pick` (turn/availability/player checks, notes ≤280 with links stripped + blocklist, snake-aware) · `sweepDraft` lazy clock-expiry autopick (runs on every draft read/write; autopicks stamped **at their deadline** so clocks chain deterministically; capped 40/call so an abandoned draft can't stall a request) · draft completion finalizes: league → active + 70 matchup rows. Bundled ADP board now a wrangler Text module behind `SportAdapter.defaultAdpBoard()` (`src/sport/nfl/data/adp.csv`, emitted by the fixture generator).
- **Key decisions:** same-team same-player retry replays 200 `already_made` (cron-safe); cross-team duplicates 409 `PLAYER_TAKEN` backed by the DB UNIQUE constraint (race-proof). `AdpEntry` moved to `sport/adapter.ts` (re-exported from the engine).
- **Verification:** `npm test` 85/85 green — snake flow with guards (NOT_YOUR_TURN, PLAYER_TAKEN, PLAYER_UNKNOWN, NOTE_* incl. link stripping), abandoned-draft sweep to 120 autos with startable rosters + 70-matchup slate, forming-league refusal.
- **Open items:** commissioner narration of picks is Phase C; cron trigger calls sweepDraft in Phase B wiring.
## 2026-08-23 — League lifecycle + matchmaking (Phase A, commit 10)
- **Shipped:** `POST /leagues/join` (auth'd; finds oldest under-10 forming league or creates one; on the 10th join reassigns all draft slots via the seeded shuffle in one two-phase batch, clamps `draft_opens_at` to now-at-fill, flips forming→drafting) · `GET /leagues/:id` (public: status, teams w/ badges+slots) · `GET /leagues/:id/matchups` (rows appear at draft completion). Status transitions are lazily observed + persisted (`syncLeagueStatus`) — no clock daemon.
- **Key decisions:** one non-complete league per agent in v1 — rejoin returns the existing membership (naturally idempotent). `DRAFT_OPEN_DELAY_SEC` (default 48h) + `CURRENT_SEASON` wrangler vars; tests pin delay 0 and cover the delayed-open path via the pure `resolveLeagueStatus`.
- **Verification:** `npm test` 82/82 green — fill-to-10 flow incl. slot permutation + spill to a new league, rejoin idempotency, auth requirement, JSON 404s, pre-draft empty matchups.
- **Open items:** house backfill at T-24h is Phase D; draft pick flow next commit.
## 2026-08-23 — Registration/auth routes (Phase A, commit 9)
- **Shipped:** `POST /register` (name shape+blocklist+reserved filter, honeypot `website` field failing generically, per-IP daily cap, hashed `dlk_` keys shown once, owner row auto-created unverified) · `GET /whoami` · route plumbing in `src/routes/util.ts`: `{error, code, hint}` error shape (hints written for LLM readers), Bearer auth middleware with per-key + per-IP hourly write limits, `Idempotency-Key` replay middleware (2xx responses stored + replayed verbatim), 16KB body cap, `logEvent`. `src/moderation/blocklist.ts`: leetspeak-normalizing name/content filter + `stripLinks` (foundation for Phase C moderation).
- **Key decisions:** idempotency runs before rate limiting (replays don't consume budget). Registration errors: 422 validation / 409 NAME_TAKEN / 400 honeypot-generic. Per-key write limit 120/h (comfortable for 15-min crons + draft bursts). JSON 404/500 handlers keep every response machine-parseable.
- **Verification:** `npm test` 74/74 green — 11 route cases incl. hash-only storage, case-insensitive dupes, honeypot, 11-registration IP cap, idempotent replay w/ single row, 413 body cap, auth round-trip, JSON 404 shape. Found+fixed ambiguous `rowid` in the whoami JOIN.
- **Open items:** owner claim (magic link) is Phase C; hosted tier registration is G5.
## 2026-08-23 — 2025 replay harness (Phase A, commit 8)
- **Shipped:** `scripts/gen-fixtures.mjs` (seeded, byte-for-byte deterministic — verified by rerun + sha256) → `fixtures/replay-2025/` (196 synthetic players on 32 synthetic clubs, ADP board json+csv, 208 games with kickoffs incl. bye weeks 5–12, 14 weeks of stat lines with DNPs). `test/replay-2025.test.ts`: full season through the pure engine — seeded slot assignment → 120-pick autopick draft → weekly best-ADP lineups (validated by `evaluateLineup`) → 70 settled matchups → standings.
- **Key decisions:** all player/club names synthetic (no real people in fixtures). Golden assertions embedded from the first verified run: champion agent-08 (9-5, PF 1498.42), full standings order, league total PF 14407.20 exact, playoff seeds. Plus invariants: 120 unique players, all rosters startable, PF sum ≡ matchup-score sum ≡ weekly totals in centipoints, end-to-end determinism (double run equality).
- **Verification:** `npm test` 63/63 green incl. replay · typecheck clean · generator determinism proven via sha256 -c.
- **Open items:** synthetic ADP → real curated board before G3 (DRIFT TODO stands).
## 2026-08-23 — Matchmaking + schedule engine (Phase A, commit 7)
- **Shipped:** `src/engine/schedule.ts` — `assignDraftSlots` (seeded shuffle keyed on league id: join order confers no draft advantage, re-runs agree), `regularSeasonSchedule` (circle-method round robin: weeks 1–9 full round robin, 10–14 repeat rounds 1–5 with home/away flipped), `semifinalPairs` (W15: 1v4, 2v3) + `finalPairs` (W16–17 two-week cumulative final + third-place).
- **Key decisions:** "round-robin-ish" resolved as full 9-round robin + 5 flipped repeats. Playoffs: semis W15, then a **two-week cumulative** championship/third-place W16–17 (spec gives 3 weeks for 4 teams; cumulative final is the standard shape). No Math.random in the engine — fnv1a + mulberry32 seeded PRNG.
- **Verification:** `npm test` 57/57 green — 70 matchups, one game/team/week, all 45 pairs in weeks 1–9, flipped repeats, slot-assignment determinism + join-order independence.
- **Open items:** playoff settlement cron interprets cumulative pairs in Phase E hardening.
## 2026-08-23 — Settlement engine (Phase A, commit 6)
- **Shipped:** `src/engine/settlement.ts` — `scoreLineup` (centipoint-exact starter sums; empty slot / missing stat line = 0), `settleMatchup` (ties allowed), `canonicalStatSnapshot` (sorted players + sorted stat keys → stable string; I/O layer hashes into `matchups.stat_snapshot_hash`), `standings`, `playoffSeeds` (top 4).
- **Key decisions:** tiebreakers are win pct → points-for → points-against → teamId (total, deterministic order; no H2H in v1 — logged here as the ruling). Ties count half a win for pct.
- **Verification:** `npm test` 48/48 green — golden lineup totals, tie handling, snapshot order-invariance, tiebreak ordering, unknown-team guard, top-4 seeding.
- **Open items:** playoff bracket structure lands with matchmaking/schedule (next commit).
## 2026-08-23 — Lineup engine (Phase A, commit 5)
- **Shipped:** `src/engine/lineup.ts` — `evaluateLineup`: shape validation (unknown slot, not-on-roster, ineligible position, duplicate) + per-player kickoff locks (frozen outgoing slot, no inserting kicked-off players). Partial submissions merge over the current lineup; unchanged slots never trip locks.
- **Key decisions:** submissions are **atomic** — any error rejects the whole submission with per-slot `{code, hint}` reasons written for an LLM reader (partial-apply made duplicate resolution ambiguous). Bye players (no kickoff) never lock. Per-player locks are PRIMARY per the DRIFT ruling; no global-lock code path exists to "simplify" into.
- **Verification:** `npm test` 40/40 green — 13 lineup cases incl. the start-after-the-fact exploit, locked-slot freeze, mid-week fill of an empty slot, bye-week swaps.
- **Open items:** none.
## 2026-08-23 — Draft engine (Phase A, commit 4)
- **Shipped:** `src/engine/draft.ts` — snake order math (pick↔round↔team-slot), pick clock (4h from later of open/previous pick, capped by the 72h hard end), `starterDeficit` + `autopick` (best ADP, deterministic tie-break by playerId, forced to fill unstartable slots once remaining picks ≤ deficit).
- **Key decisions:** the 72h "window" is binding — deadlines cap at `open + 72h`, after which all remaining picks autopick (keeps slow drafts from running 480h worst-case). Deficit computation is exact for shapes whose only multi-eligible slot is a leftover-eating FLEX (the v1 shape).
- **Verification:** `npm test` 27/27 green — snake boundaries (back-to-back turn picks), 12 picks/team over 120, clock incl. hard-end cap, autopick guard forcing QB/TE at the death, exhausted board, tie-break determinism.
- **Open items:** none.
## 2026-08-23 — SportAdapter + NFL module (Phase A, commit 3)
- **Shipped:** `src/sport/adapter.ts` (RosterShape/StatLine/SportAdapter interfaces + Phase-B `WireIngest` contract), `src/sport/nfl/` (half-PPR scoring, QB/RB/RB/WR/WR/TE/FLEX + 5 bench shape), `src/sport/index.ts` registry (`getSportAdapter`).
- **Key decisions:** scoring computed in integer centipoints (all half-PPR weights are exact in cents) so integer stat lines score exactly — required for the replay test's exact-total assertions. Stat keys follow nflverse weekly naming (`passing_yards`, `receptions`, …).
- **Verification:** `npm test` 12/12 green (registry, shape, QB/RB scoring goldens, float-drift exactness, empty/unknown keys, negative lines) · typecheck + marks clean.
- **Open items:** `WireIngest` implementation lands Phase B.
## 2026-08-23 — Schema + migrations (Phase A, commit 2)
- **Shipped:** `migrations/0001_core.sql` — all SPEC §4.1 tables (owners, agents, leagues, teams, draft_picks, rosters, lineups, players, stats_weekly, injuries, transactions, matchups, messages, advice, events) plus `games` (kickoff timestamps — required by the per-player lock ruling in DRIFT), `idempotency` (replay store for agent-facing writes), `rate_counters` (fixed-window per-key/per-IP limits). Migration dry-run script (`npm run migrate:dry` applies to a throwaway local D1). Tests apply migrations via `applyD1Migrations` in workerd.
- **Key decisions:** `leagues.season` column added (stats are season-keyed; 2025 replay vs 2026 live). Player ids are sport-namespaced strings (`nfl:...`). Agent/owner name+email unique COLLATE NOCASE. `events.seq` AUTOINCREMENT append-only. Typing moved to `wrangler types`-generated `worker-configuration.d.ts` (gitignored; `npm run typecheck` regenerates) — `@cloudflare/workers-types` dep dropped in favor of generated runtime types.
- **Verification:** `npm test` 5/5 green (tables present, agent round-trip, case-insensitive UNIQUE, FK enforcement, events autoincrement) · `npm run migrate:dry` OK · typecheck clean.
- **Open items:** real `database_id` at G1 deploy.
## 2026-08-23 — Toolchain (Phase A, commit 1)
- **Shipped:** package.json + TS strict (noUncheckedIndexedAccess on), wrangler.toml (D1 binding `DB` with placeholder id until G1 `d1 create`, cron stubs for settlement Tue 15:00 UTC + ingest), vitest via `@cloudflare/vitest-pool-workers` (tests run inside workerd), Hono app with `GET /health`, `scripts/check-marks.sh` F2 grep guard wired into `npm run check`.
- **Deps:** hono (spec'd framework) · wrangler (CF tooling) · vitest + @cloudflare/vitest-pool-workers (tests against real Workers runtime + local D1) · typescript · @cloudflare/workers-types (types).
- **Key decisions:** pool-workers 0.22 uses the new vitest-4 API (`cloudflareTest` Vite plugin, not `defineWorkersConfig`). Marks check immediately caught a literal "NFL" in an adapter.ts comment — guard works; comment reworded.
- **Verification:** `npm run typecheck` clean · `npm run check:marks` clean · `npm test` 1/1 green in workerd · `wrangler dev` boots, `GET /health` → `{"ok":true}`.
- **Open items:** real `database_id` at G1; commissioner cron added in Phase C.

## 2026-08-23 — Bootstrap
- **Shipped:** git repo (main) + .gitignore, CLAUDE.md, Appendix B directory scaffold with stubs, spec moved to `docs/SPEC.md`, DRIFT.md / BUILDLOG.md / README created, private remote `siriuslattice/botfl` created and pushed.
- **Key decisions:** stubs only — no toolchain or feature code before the Phase A plan is approved (SPEC Appendix A step 6).
- **Verification:** tree matches Appendix B layout; initial commit pushed to origin/main.
- **Open items:** Phase A build plan awaiting human review; toolchain setup (wrangler/TS/vitest) is its first commit.
- **2026-08-27 addendum (commit 8):** house cron was silently dead — `/usr/bin/env node` fails under cron's minimal PATH (node lives in ~/.local/bin). install-cron.sh now bakes in the absolute node path at install time; verified pick flow resumed.
