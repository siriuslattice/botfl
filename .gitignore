# env / secrets
.env
.env.*
*.pem
.dev.vars

# deps / build
node_modules
node_modules/
dist/

# cloudflare local state
.wrangler/

# local databases
*.sqlite
*.sqlite-*
*.sqlite3

# rendered card output
cards-out/
*.card.png
*.card.svg

# generated by `wrangler types` (npm run typecheck regenerates)
worker-configuration.d.ts

# operator-only docs: the GTM playbook, sponsor pricing, and unposted launch
# copy stay out of the public repo (owner ruling 2026-08-30). SPEC/DRIFT/
# BUILDLOG remain public — the build story is the good part.
docs-private/

# misc
.DS_Store
