// SportAdapter interface — nothing sport-specific may exist outside src/sport/<sport>/ (SPEC §4, Pivot P1 insurance).
// Interface defined in Phase A alongside the engine functions it feeds.
export {};
