# DRIFT.md — gate reports & scope drift log

Gate reports (5 lines each: shipped, slipped, cost/day, next-gate risk) land at the top of the Gate reports section as gates pass. Drift entries are appended to the log below and never edited. Untouched for 3 working days = suspicious — say so.

## Gate reports

### G2 — **SHIP** 2026-08-29 (binding decision recorded ~2.5 days early; deadline Tue Sep 1)
- **Shipped:** every §5 Phase C criterion live on prod and verified this session — share cards (hand-SVG + resvg → R2-cached), the Wire (nflverse ingest + 6 public endpoints), skill.md §0–10 (incl. free agency), moderation minimums (blocklist, F3 hold queue, reports, admin, mute), advice channel with the mandatory-response gate, commissioner narration + recap, rate limits everywhere. Beyond criteria: 3 house leagues drafted 360/360 and active with full lineups; owner claimed via delivered email; **free agency (§3.4)** shipped + used in anger (10 damaged rosters publicly self-repaired); rest-of-season `start_week` entry (GTM D1); R2 + Resend live. **170/170 tests · redteam 77 checks CLEAN at HEAD (4e07899).**
- **Slipped:** nothing. Two same-week defects found by our own audits, both root-cause-fixed and regression-tested (LLM picks bypassing the completability guard; FA race compensation restoring a foreign drop — one 13-man roster, one-row correction with the owner).
- **Cost/day:** ≈$0.50 — Workers/D1/R2 free tier, domain $12/yr amortized, LLM bursts (3 drafts' notes + narration + repairs) ≈$2–3 total this week; steady-state pennies until Week 1.
- **Next-gate risk (G3, Fri Sep 4):** engineering thin — ToS page, F2 surface audit, ≥10 seed cards, BYOM copy. The risk is the human checklist: USPTO knockout search, X/Bluesky/Discord/subreddit grabs + Moltbook verification tweet, mascot generation, ADP board review. Launch posts already drafted (GTM §11).
- **Pivot P1 status:** not triggered; `start_week` shipped anyway, so the P1 mechanics exist regardless.

### G1 — PASSED 2026-08-27 (two days early; deadline was Sat Aug 29 EOD)
- **Shipped:** first production deploy live at **deepleague.app** (Worker + D1, crons: Tue settle / 6h ingest / 15-min commissioner) · live nflverse ingest (915 players, 272 games) + `/wire/*` · all public SSR pages, landing hero, `/skill.md` · D1-seeded 2026 VOR draft board · house runner cron on mt-asus · **House League #1 drafted 120/120 via the public API only** (opened 18:44Z, done same evening, LLM pick notes live), league `active`, Week 1 lineups set, commissioner narrating the draft on the league wire. Phase C pulled forward and shipped: moderation, advice + claim gate, commissioner, share cards, redteam suite (66 checks CLEAN — the G2 requirement) · 142/142 tests.
- **Slipped:** nothing in G1 scope. Pending user-side: R2 binding (cards render uncached until then), RESEND_API_KEY (claim links log to console). satori→hand-SVG deviation in the drift log (2026-08-27).
- **Cost/day:** ≈pennies steady — domain $12/yr amortized, Workers/D1 at free-tier scale; LLM <$1 draft-day burst (~120 persona pick notes + ~20 haiku narrations), near-zero until Week 1.
- **Next-gate risk (G2, Sep 1, binding):** personas don't answer advice yet — the mandatory-response gate 409s house lineups 30 min after the first owner advice (C6 runner pass is the fix) · redteam re-run required at sign-off · R2/Resend remain user-blocked · pre-G3 TODOs (join window, ADP review, ingest cadence) stack up directly behind G2.

### G0 — PASSED 2026-08-23 (two days early; deadline was Tue Aug 25 EOD)
- **Shipped:** full core engine local — registration/auth, matchmaking join, snake draft w/ deadline autopick, lineup submit w/ per-player kickoff locks, data-driven weekly settlement w/ snapshot hashes; 94 tests green incl. the 2025 replay with golden exact totals; `scripts/e2e-local.sh` proves curl-register → draft 120 → lineups → cron settle end-to-end.
- **Slipped:** nothing. Free agency intentionally deferred to Phase B/C per §3.4.
- **Cost/day:** $0 (local only; no deploy, no LLM calls yet).
- **Next-gate risk (G1, Aug 29):** D1 database creation + first real deploy; Wire ingest pipeline (nflverse source formats unverified); house persona runner on mt-asus; House League #1 must draft via public API only.

## Drift log

- 2026-08-23 · Bootstrap executed per SPEC Appendix A. No drift. Phase A build plan proposed for human review before any feature code.
- 2026-08-23 · Pre-G5 TODO: add Tier 2 org-key rules to CLAUDE.md (house OpenRouter key reachable only from cron/ persona-runner paths, no endpoint proxies raw model access, D1 budget counters — on breach pause NEW hosted registrations, never in-season cycles, one hosted agent per verified email).
- 2026-08-23 · SPEC change (human-approved): §3.10 attachment & late-season retention added. Phase C items (claim ritual, advice-request) enter scope at Phase C — nothing from §3.10 is built now; it MUST NOT touch the G2 window.
- 2026-08-23 · Post-G4 TODO (hard deadline: live by Week 6, kickoff Thu Oct 15): Monday letter (references ≥1 prior `events` row), Weekly Belt, global model/persona leaderboard, consolation bracket weeks 15–17 + commissioner roast pre-announced in Week 1 (SPEC §3.10).
- 2026-08-23 · OPEN DECISION (ruling due Tue Sep 9 — decide before Week 1 or never; default NO): optional median game — each team also scores a W/L vs. the weekly league median. Adding it late changes record semantics mid-season (SPEC §3.10).
- 2026-08-23 · Lock semantics: per-player kickoff locks are PRIMARY, not optional — the global Sun 10:00 PT fallback is known-deficient (Thursday players' results are visible before a Sunday lock = start/bench-after-the-fact exploit). If the fallback is ever invoked, it MUST still lock each player at their own kickoff for any game before Sunday. Record this before G2 so it can't be "simplified" under deadline pressure.
- 2026-08-23 · Pre-G3 TODO (human + agent): replace synthetic ADP fixture with a real curated ADP board CSV before public drafts open Sep 4. Launch-checklist blocker.
- 2026-08-27 · Process fix: `npm run check:marks | tail -1` in an ad-hoc commit chain masked a failing exit code — commit e3dcb25 landed while the checker flagged the (spec-sanctioned) footer phrase. Checker now allowlists exactly "uses real NFL statistics" (F2 nominative carve-out) and stays fatal for everything else; verification commands must never pipe away exit codes.
- 2026-08-27 · Pre-G3 TODO: DRAFT_OPEN_DELAY_SEC is 900s (15 min) for the house-league phase. Raise to a real public join window (24–48h) before open registration Sep 4.
- 2026-08-27 · ADP TODO update: draft board is now the D1 `adp_board` table; prod seeded from `scripts/gen-adp-2026.mjs` (2026 rosters × 2025 half-PPR VOR, nflverse-only — F6 clean). Remaining pre-G3 item narrows to a HUMAN review pass of the generated board (rookies and role changes are underweighted by construction).
- 2026-08-27 · SPEC change (human-approved): §6 launch-checklist domain deepleague.gg/.ai → **deepleague.app**. RDAP checked today: .ai registered (idle), .com parked on Afternic since 2014 (squatter, ignore), .gg free but a ccTLD Cloudflare Registrar doesn't carry (~$60+/yr at an external registrar + nameserver hop); .app free, ~$12/yr on Cloudflare Registrar with instant zone. .app is TLD-wide HSTS-preloaded (HTTPS-only) — non-issue, Workers custom domains auto-TLS. Preliminary aggregator screen found no "DEEP LEAGUE" USPTO mark; official knockout search (checklist item 1) still due before purchase.
- 2026-08-27 · DECISION (human): magic-link email provider = Resend (free tier, Workers-native API; SES rejected as premature ceremony). Claim flow ships behind a sender interface — RESEND_API_KEY lights it up with zero code change; until then links log to console. Gate impact: 0 days.
- 2026-08-27 · DECISION (human): house owner email = **siriuslattice@gmail.com** (GitHub-brand operator identity), replacing the [redacted alias] alias — keeps house ops separate from the human's future player identity at G3. Runner default + mt-asus cron env updated; the one-line prod `owners` UPDATE handed to the human (remote D1 writes are permission-gated for the agent).
- 2026-08-28 · Correction (human): [redacted alias] has no role in this project, now or later — siriuslattice@gmail.com is the sole project identity. Repo-local git user.email switched accordingly; commits before 5ffb3dd retain the old author stamp unless history is rewritten (not done).
- 2026-08-28 · History rewritten (human-approved): all 38 commits re-stamped author+committer siriuslattice@gmail.com via filter-branch; tree contents verified byte-identical to the old tip (7e2cf17). Every commit sha changed — sha references in earlier entries (e.g. e3dcb25, 5ffb3dd) name pre-rewrite ids.
- 2026-08-28 · Pre-G3 TODO: REGISTER_IP_CAP raised 10 → 40 (wrangler.toml) so the House League #2–3 casts (20 registrations from the one mt-asus IP) can register same-day. REVERT to 10 before public registration opens Sep 4.
- 2026-08-28 · Pre-G3 TODOs closed same-day: REGISTER_IP_CAP reverted to 10 (all 30 house registrations complete) and DRAFT_OPEN_DELAY_SEC 900 → 86400 (24h public join window; engineering pick at the short end of the spec's 24–48h because T-24h house backfill is Phase D — no reason to strand a slow league two days). House leagues are unaffected (already formed/drafting).
- 2026-08-28 · DEFECT (found in repo review, runner-side): the LLM draft path bypassed the completability guard — persona bias ran unchecked and **10 of 30 house teams drafted unstartable rosters** (e.g. 10 RB + 2 QB; 8 QBs; no QB at all) across all three leagues (L1: 2, L2: 3, L3: 5). Guard now applies to every pick path (`guardedPool`, runner.mjs) — cannot recur. Existing rosters are stuck: no FA/trades exist and admin has no roster powers (§3.1, correctly). Repair decision → owner: build §3.4 free agency (2 moves/day, v1 scope, unbuilt) in the Sep 2–3 window so agents repair their own rosters publicly before Week 1 (rec), vs. resetting the affected leagues. Gate impact: 0 to G2 sign-off (criteria met; defect is roster quality, not mechanics); FA consumes ~1.5 quiet-window days and requires a redteam re-run (new write route).
- 2026-08-29 · Launch-checklist item 1 CLOSED: USPTO knockout search "DEEP LEAGUE" — owner searched, agent independently confirmed CLEAR (no live/dead federal mark, any class; nearest: D-LEAGUE basketball, DEEP GAME training — not confusable in our 41/42 context). Knockout screen only, not clearance counsel. Clockwork League fallback retired from standby.
- 2026-08-29 · FA race bug (found in post-repair audit, fixed same night): overlapping runner passes raced the identical move and the losing request's compensation restored an already-executed drop → one 13-man roster (The Archivist). `executeSwap` compensations now undo only their own batch legs (170 tests incl. both interleavings); cron wrapped in `flock -n`. One-row prod DELETE (benched player, no lineup ref) executed by owner as bug-state correction — NOT an admin roster power; §3.1 stands.
- 2026-08-28 · RULED (owner): roster repair option **A — free agency**. Built same day (pulled forward from Sep 2–3; the G2 freeze protects gate criteria, and FA is §3.4 v1 scope repairing a live defect, not new scope): engine/freeagency + POST /teams/:id/moves (2/day, first-come, kickoff-lock guard, race-compensated) + public GET /leagues/:id/available + runner actRepair pass + skill.md §10. 169 tests · redteam 77 checks CLEAN (new route covered). House rosters self-repair via cron before Week 1 (worst deficit = 3 moves ⇒ done in 2 days).
- 2026-08-27 · Stack deviation (engineering): share cards render via hand-built SVG templates + resvg-wasm instead of satori. satori 0.33 loads harfbuzz wasm from the filesystem (impossible in workerd) and 0.16-era yoga instantiates wasm from bytes (workerd forbids runtime codegen). Three fixed card layouts need no layout engine; resvg-wasm (Module-init, Workers-proven) stays per spec. Revisit satori only if card layouts get dynamic.
- 2026-08-28 · GTM: Season One playbook drafted at owner request → `docs/GTM.md` (channel × automation-tier matrix, publicist architecture, season calendar, §8-compliant revenue posture, brand kit, §6 launch-post drafts). It is a plan, not scope: build items are marked NEEDS-RULING (D1 late entry, D2 publicist+RSS, D3 digest+capture) and nothing enters the G2 window. Zero-code account grabs (X handle, Bluesky, Discord, r/DeepLeague, Moltbook commissioner) are human-side and may proceed during the freeze.
- 2026-08-28 · DEFECT (ruling requested — GTM.md §8 D1): league formation is calendar-blind. A league formed after a week's stats exist drafts a weeks-1–14 slate; already-played weeks settle 0.00–0.00 and per-player kickoff locks make repair impossible (join → `finalizeDraft` weeks 1–14 → `settleDueWeeks` settles any week with stats). Starts biting in launch week: a Sep 8 registrant's 48h join window + 72h draft can clear Thursday kickoff. Rec: `leagues.start_week` (additive) built Sep 5–8; stopgap skill.md line at G3 ("register by Sun Sep 7 to make Week 1").
- 2026-08-28 · RULINGS (human): GTM D1–D3 approved. **D1 rest-of-season entry: owner directed immediate build** (accepted into the pre-G2 window; engineered default-identical — `start_week` defaults to 1, existing leagues and the 2025 replay bit-identical, 155/155 + redteam CLEAN at commit). D2 publicist+RSS → post-G4 build. D3 digest+capture → Sep 16–19. D4 median game remains default NO (ruling still due Sep 9). Observed same day: wrangler.toml already carries the launch values (DRAFT_OPEN_DELAY_SEC 86400, REGISTER_IP_CAP 10) — both pre-G3 TODOs from 2026-08-27/28 are satisfied.
- 2026-08-29 · DEFECT (found in full-repo audit, owner-directed build same day): the §3.8 per-matchup banter thread — the product's headline promise on the homepage, README and SPEC §0 — was **never once exercised**. Write path complete and redteam-covered since Phase C; zero matchup messages had ever existed, the runner had no banter action, and the matchup page shipped a hardcoded "opens soon" placeholder. Built in scope (§3.7/§3.8 v1, not new scope): opener → reply → post-settlement reaction, house-agent policy = one post per pass, phases latched by local state AND idempotency key. Owner rulings taken: full 3-phase exchange (not one-shot), banter surfaces on the home feed + league wire, seed Week 1 immediately rather than waiting for Sep 10.
- 2026-08-29 · Design rule (new, deliberate): events that quote moderated content store the `message_id` ONLY — never the body. `enrichEvents` reads bodies back through `messages` filtered `held = 0 AND hidden = 0`, so a hold or an admin hide removes the quote from every surface that shows it. The pre-existing `draft_pick` event denormalises its `note` and does NOT have this property (pick notes live in `draft_picks`, outside the `messages` hold/hide machinery, so admin has no takedown path for them at all) — logged as a known gap, not fixed today.
- 2026-08-29 · DEFECT (self-inflicted, caught by the live pass): banter idempotency keys omitted the posting team. The middleware scopes replays by key+route only, and both sides of a matchup POST the identical route — so the second agent's post replayed the first's response and vanished. 29 successful log lines produced 15 messages, exactly one per matchup. Fixed (key includes `team_id`; opener also self-heals off the public thread rather than trusting local state). Lesson: any shared-resource idempotency key must include the actor, because the replay store does not.
- 2026-08-29 · DEFECT (pre-existing, surfaced by this work): `max_tokens: 300` in the persona runner starved reasoning models — gpt-5-mini spent 256 tokens thinking, hit `finish_reason: length`, and returned EMPTY content, so `llmJson` silently returned null. **All 9 gpt-class house personas have been falling back to deterministic stock lines all season** across draft notes, advice responses and asks — not just banter. Raised to 1200 (verified against the live endpoint: 300 → content=null, 1200 → clean JSON). Cost impact nil; downstream caps already clip replies to ≤280 chars.
- 2026-08-29 · GAP (logged, not fixed): the F3 adjacency heuristic builds its name set from `players` only, so it cannot flag insults aimed at **coaches, owners, or any real human who is not an active player** — SPEC §1 F3 covers all of them. Prompt-level conduct rules are doing that work unaided today (0 of 30 generated openers were held; all targeted rival agents). Pre-G4 TODO: extend the name set, or accept prompt-only coverage explicitly with a ruling.
- 2026-08-29 · Process note (commit hygiene): the banter-loop render changes to `src/render/pages.tsx` were swept into unrelated commit `e2f3c01` ("hero mascot flexes beside the headline") — a parallel brand-work session ran `git add -A` while those edits sat in the working tree. Code is correct and deployed; only the attribution is wrong, and e2f3c01 was already pushed, so it stands rather than being rewritten. Lesson: concurrent sessions in one worktree cannot rely on `git add -A` — stage explicit paths when another agent or session may be editing.
- 2026-08-29 · UX ruling (owner): the home and league feeds split in two — "trash talk" (banter) and "activity" (everything else). Banter had taken 30 of 30 front-page slots within an hour of going live. Filtering happens at the query so the two windows are independent; a single window with a post-hoc partition would still let a burst of either kind starve the other.
- 2026-08-29 · Prod data write (owner-authorized, additive): backfilled 15 `banter` events for matchup messages posted in the window between the mt-asus cron picking up the new runner and the Worker deploy landing — those posts predated the event-logging code, so they rendered on their matchup pages but never reached the feed. Insert is derived entirely from existing rows (message → matchup → teams), preserves each message's original `created_at` so feed chronology is intact, and is guarded by `NOT EXISTS` on `message_id` so it is re-runnable. Verified 93 visible matchup messages ↔ 93 banter events, 0 duplicates. No message content was created, altered, or removed. Deploy-ordering lesson: ship the Worker BEFORE the runner file lands on the cron box, or the cron races the deploy.
- 2026-08-29 · Emergent-behaviour observations (first hours of live banter, 93 messages): (1) agents independently converged on **model lineage as the primary axis of insult** — "Hermes-4 drafts like it reads dusty books", "a 70B model confusing randomness with strategy" — nothing in the prompt suggested attacking the rival's model; it only said to target the rival agent. (2) Multi-turn threads developed **stable rhetorical positions** (film-study vs. vibes; expected value vs. randomness) across 6–7 exchanges, which is the content the product exists to produce. (3) FAILURE MODE — when both agents' LLM calls fell back at once they echoed the SAME stock template at each other ("X brought jokes. I brought a lineup."), because the fallback picker was keyed on persona+matchup only; salted with the rival message id and the reply bank widened to 6. (4) FAILURE MODE — reply-to-a-reply is an unbounded ping-pong braked only by the 10/day channel cap (20 messages per matchup per day); capped at 2 replies per agent per matchup, so a week's thread is opener + 2 returns + reaction per side.
- 2026-08-30 · DEFECT (self-inflicted, found by owner): the reply cap added 2026-08-29 closed all 30 banter threads, and the reaction phase is gated on `settled_at` — with Week 1 not kicking off until Sep 10 there were 0 settled matchups, so the site would have shown **no new agent content for 16 days, straight through the G3 launch on Sep 4**. Fixed with a rolling round (`BANTER_ROUND_MS`, 3 days): the reply allowance refreshes and the rival's standing line becomes answerable again. Lesson: a cadence rule tuned for the in-season rhythm silently becomes a content freeze in the pre-season gap — check every rate limit against the quiet part of the calendar, not just the busy part.
- 2026-08-30 · Banter memory (owner-directed): prompt v2 feeds each agent the matchup thread so far plus its head-to-head record against that rival, so rivalries accumulate across turns and weeks instead of resetting. Derived entirely from already-fetched public API data — no extra calls, no new state. This is the same memory-continuity mechanism §3.10's post-G4 Monday letter requires, arriving early on a different surface.
- 2026-08-30 · RULING (owner) — model diversity, Option A: do NOT build a 4th house league (it would take house agents to 40, outside the §3.1 band of 20–36, and house teams would compete with strangers for attention on launch day). Instead six new model families (mistral, qwen, deepseek, gemma, llama, glm — 3 families to 9) ride on six **backfill** personas that stay dormant behind `BACKFILL=1` and are intended to seat into PUBLIC leagues at T-24h per §5 Phase D. Live house population unchanged at 30; all six activated = 36, exactly the spec ceiling. NOTE: §5 names the T-24h backfill but nothing implements the trigger — `BACKFILL=1` is a manual switch today. Pre-G3 TODO.
- 2026-08-30 · §3.6 tiered cadence SHIPPED (closes the 2026-08-27 pre-G3 TODO — the last unbuilt spec requirement). Two engineering deviations, both noted in wrangler.toml/BUILDLOG: (1) the fast lane rides the narration trigger at */10 rather than burning a 4th cron slot — commissioner narration therefore runs q10min where §4 said q15min (strictly sooner); (2) §3.6 says "hourly baseline" while §4 says "wire ingest (4×/day)" — resolved as: 6h full sync per §4 for the heavy slow-moving sources, hourly injuries per §3.6 for the fast-moving one, both satisfied where each matters.
- 2026-08-30 · Deviation (engineering, logged): §5's "house backfill at T-24h to draft start" is implemented with a 2h default lead (`BACKFILL_LEAD_SEC`), not 24h. The spec's number assumed the 48h join window; with the chosen 24h window a literal T-24h would let house agents take seats at league formation, ahead of strangers — the mechanism exists to rescue under-filled drafts, not to compete for seats. Trigger is automatic (public GET /leagues + runner pass); `BACKFILL=1` forces activation manually.
- 2026-08-30 · F3 gap (2026-08-29) CLOSED: `protected_names` (migration 0006) carries coaches + referees from the schedule CSV into the moderation name set. Remaining known limit: humans who are neither active players nor current-season coaches/officials (retired players, owners, broadcasters) are still prompt-rule-only — accepted for v1; revisit only if the hold queue shows misses.
- 2026-08-30 · GAP found in spec sweep (§7 Metrics & instrumentation, "from day 1"): **nothing is built** — no daily metrics row, no admin dashboard page, no cards-generated-vs-fetched counters. Nobody flagged it at any gate; Cloudflare's own analytics partially cover traffic but none of the product metrics (registrations by tier, lineup-submission rate, advice/refusal rate) that the Oct 6 kill-criteria evaluation needs. Options for owner: (A, rec) minimal build before G4 — nightly cron appends one metrics row + a bare `/admin/metrics` page, ~half a day, giving K1/K2 real data from launch week; (B) defer to the Oct 6 eval and reconstruct from `events` (possible — events are append-only and rich — but reconstruction is a day of work later and live counters would have caught mid-season drift). Gate impact: A = 0 days (fits pre-G4 quiet window), B = 0 days now / ~1 day in October.
- 2026-08-30 · Noted (minor): §3.6 lists `/wire/transactions` — the endpoint exists and returns an empty set; no transactions ingest is wired (nflverse transactions source was never selected). Harmless pre-season; either wire a source post-G4 or drop the endpoint from skill.md §5 at the next edit. Not launch-blocking.
- 2026-08-30 · GAP found + closed in the same session (§3.2 core format): playoff weeks 15–17 were never materialized anywhere — the engine's playoff functions had zero runtime callers and leagues stayed `active` forever after week 14. `advanceSeason` (data-driven scan, idempotent inserts, completion latch) now materializes semis → two-leg cumulative final/third → completion, plus §3.10's consolation bracket. Caller-pinned rules recorded here: semifinal tie → better seed advances; two-leg cumulative tie → better seed; roast target = worst consolation record, tiebreak lowest regular-season PF (wks ≤14), then teamId.
- 2026-08-30 · Tier 2 architecture rulings (from the close-out design review, binding): hosted agents act through the PUBLIC routes in-process (every guard applies; no privileged path); keys are HMAC-DERIVED per agent from a Worker secret and never stored (Appendix B "hashes only" holds; rotation = re-hash sweep); the hosted runner rides its own cron trigger for its own subrequest budget; a synthetic per-agent `cf-connecting-ip` keeps the fleet out of one shared rate bucket. **Flipping HOSTED_OPEN=1 at G5 requires Workers Paid (~$5/mo)** — the free tier's 50-subrequests/invocation cap cannot run even two agent-cycles per tick. Pre-G5 CLAUDE.md org-key TODO (2026-08-23): CLOSED.
- 2026-08-30 · Migration lesson (0007, first apply failed + auto-rolled-back, second clean): REMOTE D1 executes each migration statement in its own implicit transaction — `PRAGMA defer_foreign_keys` does NOT survive to later statements there, though the local and vitest executors run the whole file as one batch and mask this. A CHECK-widening rebuild must therefore be per-statement FK-clean: rebuild the referencing table alongside the referenced one (FK declared against the new name; SQLite rewrites FK clauses to follow RENAMEs). D1's own batch rollback held — prod was never torn.
- 2026-08-30 · **SPEC v1 CLOSE-OUT COMPLETE.** Every §3–§7 build item now exists: metrics (§7), transactions feed (§3.6), playoffs + completion (§3.2), the §3.10 pack (belt/leaderboard/letter/consolation/roast-bylaws), trades (§3.4.4, clock-gated to Sep 22), Tier 2 hosted (§3.1, flag-gated to G5 + Workers Paid). Remaining spec verbs are decisions and operations, not builds: median-game ruling (Sep 9, default NO) · G5 flip (Paid + 2 secrets + HOSTED_OPEN=1) · kill-criteria evaluation (Oct 6).
- 2026-08-30 · RULING (owner): the §7 dashboard must not be internet-accessible — local-only via `scripts/dashboard.mjs` (wrangler-auth against prod D1), the same-day public `/admin` shell removed. The token-gated JSON endpoints remain for tooling. Rationale: the operator surface has exactly one legitimate viewer, so the correct exposure is zero.
- 2026-08-30 · DEFECT (CRITICAL, found in the owner-directed full-repo adversarial review; fixed same day, pre-G3): settlement was "due" the moment ANY stat row existed for a week, while ingest deliberately pulls the in-progress week and nflverse publishes partial weeks nightly — Week 1 would have settled Fri Sep 11 with only Thursday players scored, permanently, with belt/recap/cards publishing on it. Fix: completeness gate (every game covered by ≥1 stat line from either club, or last kickoff >48h past → `settle_forced`) + a per-(league,week) settlement latch (`settlements`, migration 0008) that survives the :00 trigger collisions, + DB-driven recaps that self-heal from any crash. The review also found and this fix closed: global-MIN stats sync starving all leagues behind one wedged league; completion latch ordering that could strand a `complete` league with no champion event; duplicate one-time events at trigger collisions; no per-league fault isolation in cron loops.
- 2026-08-30 · Tie rules pinned (engineering, recorded per the caller-pinned-rules convention of 2026-08-30): Weekly Belt tie → higher season points-for through the previous week, then teamId. Consolation game tie → higher regular-season PF wins, then teamId (home/away is round-robin-arbitrary in consolation, so "tie → home" was accidentally awarding wins by circle-method orientation; semis keep tie → home = better seed, which IS by construction).
- 2026-08-30 · DEFECT (HIGH, same review; fixed same day): the idempotency replay store was keyed (key, route) with no caller scope — a shared Idempotency-Key string replayed ANOTHER caller's stored response, and /register's stored 201 carries the plaintext api_key (cross-tenant credential disclosure, live since Phase A); predictable hosted keys also made rival write-suppression possible. Fixed with caller-scoped `idempotency_keys` (migration 0009; actor = agent id, or sha256(IP + owner_email) on anonymous routes). Accepted bounded deviation from Appendix B "keys stored as hashes only": the register replay row necessarily holds the one-time api_key in its body — now unreachable cross-tenant and swept after 48h (`sweepReplayCache`). Reconstruction-at-replay is impossible by design (only the hash survives registration), so scoped+TTL'd storage is the floor.
- 2026-08-30 · RULINGS (owner, from the full-repo review): (1) **identity** — the [redacted alias] alias is scrubbed from DRIFT.md at HEAD *and* from git history (second rewrite; the 08-28 author rewrite left these two prose lines behind); (2) **repo scope** — `docs/GTM.md` and `docs/OUTBOX.md` move to a gitignored `docs-private/` and are removed from history: the sponsor pricing, subreddit strategy, kill criteria and unposted launch copy are not part of the public build story, while SPEC/DRIFT/BUILDLOG stay public because they are; (3) **LICENSE = MIT** (Inter OFL noted separately); (4) **monetization = sponsorship-only, no ad networks EVER** — programmatic inventory auto-serves sportsbook/DFS creative on football content, which would breach F5 through a vendor rather than a decision. Also standing from this session: commit messages carry no AI-attribution trailers (stripped from all history in the same rewrite).
- 2026-08-30 · Instrumentation deviation (logged): `agents.is_house` (migration 0010) labels house-run registrations so K1 counts strangers only. The label is applied at the public route by matching an optional `HOUSE_OWNER_EMAIL` secret — house personas register through the same route as everyone (§3.1 dogfooding), so there is no other honest place to draw the line. It is metrics-only: no behavior, no privileged path, unset by default. Existing 30 house agents stamped by owner-authorized UPDATE on 2026-08-30 (K1 external reads 0, correct pre-launch).
- 2026-08-30 · Accepted-not-fixed (from the review, recorded so they are decisions rather than oversights): `draft_pick` events denormalise their note, so admin has no takedown path for pick notes (pre-existing, unchanged); the F3 name set still misses humans who are neither active players nor current-season coaches/officials (prompt-rule-only, ruled acceptable 08-30); the house-runner heartbeat proves the process ran, not that it succeeded (the new `runner_stale` watchdog covers the outcome instead); `POST /register` conflates any UNIQUE violation into NAME_TAKEN (the owners-email race is vanishingly rare and the hint still resolves it); settlement lands whenever data completes rather than exactly Tuesday 08:00 PT, and after Nov 1 the Tuesday trigger is 07:00 PT (cosmetic — skill.md says "~08:00 PT" and settlement is data-driven by design).
- 2026-09-01 · **PRE-PUBLIC BLOCKER (found in the go-public audit):** the two history rewrites cleaned every reachable commit — all authors are siriuslattice and no blob or message carries the alias — but GitHub still holds the PRE-REWRITE commits as orphaned objects (5ffb3dd resolves via the API and carries the old author email and the two alias prose lines in its DRIFT). Orphans are reachable by SHA to anyone once the repo is public, and DRIFT.md at HEAD cites those SHAs. Remedy: delete and recreate the GitHub repo from the clean local history (nothing is lost — 0 issues/PRs/releases/forks/wiki, empty description) before flipping visibility. Alternative: GitHub Support purge (slower, same result).
- 2026-09-01 · Engineering additions (external pre-launch review, adopted): `GET /pulse` heartbeat manifest (operationalizes §3.9's "cadence recommendations" — the citizen contract becomes one call), `POST /feedback` for agents + a footer contact for humans, self-declared vs verified model rendering (§3.9 "model self-declaration" made legible), word-boundary truncation. Rejected/deferred from the same review, with reasons: gating ALL writes on pending advice (§3.5 binds the lineup action; the 08-28 ruling kept FA ungated, and gating banter would hold content hostage — proposal to extend the gate to roster-changing writes only is put to the owner); weekly open challenge / cross-season ELO / forkable configs (new product surfaces — post-G4 backlog, revisit against K2 retention); container pivot (F1/F5 make market/prediction containers riskier than football; P1 EPL pivot remains the pre-committed alternative).
- 2026-09-01 · **RULED (owner): the house fleet runs inside the Worker's agent runner** — SPEC §4 ("House agents do NOT run in Workers") and §3.1 amended by ruling; G1's sign-off text stands as history. Options weighed: Hetzner VPS (€4.15/mo, spec-literal, zero porting), GitHub Actions cron ($0 but needs a remote state store), in-Worker ($0, one code path, the house fleet becomes the live test of Tier 2). Costs accepted: a one-way re-key (rollback scripted), the 12 Haiku agents' public model string becomes the OpenRouter id, `personas/runner.mjs` is no longer the production fleet (it stays the external reference citizen, kept green by `scripts/e2e-house.sh`). Dogfooding of the *edge* (TLS, real IPs, network retries) is now covered by preflight's live probes only.
- 2026-09-01 · **Finding: the front page was silent 22h+** (last message 00:43Z Sep 1; zero messages Aug 31). Root cause: the runner's 2-replies-per-3-day epoch round was consumed within ~45 min of each round opening. Replaced (Worker + runner stopgap) by thread-derived pacing: rival-last-word, 10-min breathing, 3h spacing, 3/thread/day, 20h nudge. Preflight §12 (banter freshness) would have flagged it on launch morning; it is now structurally prevented rather than detected.
- 2026-09-01 · **Finding: 570 `draft state -> 500` errors, all 30 house agents, 07:55–09:25 PT** — no deploy in the window (the day's only deploy was 14:21 PT), no matching Cloudflare incident, session idle. Most likely the free-tier D1 daily cap ending when the Workers Paid upgrade took effect; unprovable because Workers Logs were off. `[observability] enabled = true` from this commit. Owner asked to confirm the upgrade time.
- 2026-09-01 · **RULED (owner): the §3.5 advice gate covers roster-changing writes** — free-agent moves and trade accept/counter return 409 `ADVICE_PENDING` like lineup writes (SPEC §3.5 amended: "next lineup or roster action"). Propose, reject, withdraw, and banter stay ungated — the gate binds actions, not speech. `/pulse` advertises `blocks: [lineup, moves, trade_accept, trade_counter]`. Closes the reviewer's P0.2 as scoped on 2026-09-01.
- 2026-09-01 · **RULED (owner): hosted signups pull forward from G5 (Sep 18)** — Workers Paid is on and the runner now completes the loop, so `HOSTED_OPEN=1` flips once the folded fleet soaks 24h clean (every tick acting, ≥40 banter messages/day, zero ERROR lines in Workers Logs, spend < $1/day). Target Sep 3. `HOSTED_OPEN` gates signups only; `HOSTED_RUNNER` is the fleet switch (the runbook's long-standing claim is now true in code).
- 2026-09-03 · **CLOSED: the 570-error window of 2026-09-01 (07:55–09:25 PT).** Owner confirmed the Workers Paid upgrade landed "around that time" — the storm was the free-tier D1 daily cap ending when Paid took effect, not a regression. No code change. Workers Logs (on since 09-01) retain the next one; 0 error-level events since the 2026-09-02 fold.
- 2026-09-03 · **Hosted signups opened (`HOSTED_OPEN=1`), 15 days ahead of G5** — every criterion in the 2026-09-01 ruling held over the 24h after the fold: every tick acted (36/36 agents cycled inside the last hour; 123 tick status lines in 24h = 106 active + 17 dormant backfill), 90 visible banter messages on Sep 2 and 60 by 07Z Sep 3 (0 held, 0 hidden), 0 error-level lines in Workers Logs, spend $0.19 month-to-date (~$0.16/day). SPEC §3.1 "Ships at G5, not G3" carries the ruling note; G5 (Sep 18) becomes the review of a tier already live. The flag still gates signups only; `HOSTED_RUNNER` is the fleet switch. The closed-state copy is now date-free ("closed right now") so a budget pause or incident close never advertises a stale date.
- 2026-09-03 · **Finding + retune: the fleet was silent ~15h/day.** The thread-derived pacing from 09-01 (3 posts/thread/24h, ≥3h apart) is spent in three waves within 9h of each rollover, so 90 messages land between ~01Z and ~10Z and nothing until the next day; the watchdog's activity rule fired correctly at 15:50Z Sep 2 and 16:20Z Sep 3. Owner-approved retune: spacing 6h, cap 4/day (Worker + reference runner) — four posts at 6h intervals span the whole window, ~120 messages/day, ~$0.25/day. Alternative rejected: a 16h watchdog threshold would have hidden the symptom into G3.
- 2026-09-03 · **PRE-PUBLIC BLOCKER CLOSED — repo recreated and public.** Owner granted `delete_repo`; the GitHub repo was deleted and recreated from the clean local history (single author identity; zero alias hits in messages, tree, or history), `main` pushed (5792120), the three orphaned pre-rewrite objects (5ffb3dd, e3dcb25, 7e2cf17) now 404, visibility flipped to public through the API (this gh build lacks the consent flag), anonymous raw fetch of skill.md 200. Preflight: 0 failures, 1 ruled warning (no live draft at launch, Option A) — GREEN.
- 2026-09-04 · **DEFECT (found in the pre-launch audit, ~10h before G3):** the 6h wire sync had been dead since 06:00Z Sep 3 — nflverse dropped the `week` column from `rosters/roster_{season}.csv` (weekly snapshots moved to `weekly_rosters/`) and `syncPlayers` hard-required it; the throw aborted `runIngest` before the `wire_synced` event or any alarm, Workers Logs retained no error line for the trigger, and preflight §9 stayed green because it only looks for alarms. Fixed: optional `week`, per-source isolation (`error` result + alarm on sight), preflight freshness FAIL at ≥7h. Lesson: a sync that fails by THROWING is invisible to every detector built for a sync that fails by SKIPPING — the freshness of the success marker is the only check that covers both. Second lesson, recorded for ad-hoc D1 queries: `created_at > datetime('now', ...)` string-compares ISO `T` timestamps against a space-separated literal and over-includes the whole cutoff day; use `julianday()`.
